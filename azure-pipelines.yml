trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'
- bash: 'npm install -g @angular/cli'
- bash: 'npm install'
- bash: 'ng lint'
- bash: 'ng test'
- bash: 'ng build --prod'